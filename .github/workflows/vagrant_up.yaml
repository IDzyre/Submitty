name: Vagrant Up

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vagrant-up:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3
      # Installing this is skipped in CI as using it was causing some corruption in
      # attempting to mount the shared folder. The guest and host guest additions
      # versions are close enough that we are fine without it.
      # - run: vagrant plugin install vagrant-vbguest
      # - run: NO_SUBMISSIONS=1 vagrant up ubuntu-22.04
      # - name: Validate image
      #   run: curl --show-error --fail --include http://localhost:1511
      - name: Save to cloud 
        run: >
            request = $(curl
            --request POST
            --header "Content-Type: application/json"
            https://app.vagrantup.com/api/v1/authenticate
            --data '
              {
                "token": {
                  "description": "Login from cURL"
                },
                "user": {
                  "login": "reapernsgaming",
                  "password": ${secrets.VAGRANT_PASSWORD}
                }
              }
            ')
            
            echo "le response is $response"